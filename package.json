{
  "name": "graphql-api-blog",
  "version": "1.0.0",
  "description": "A sample GraphQL API for learning purposes.",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "build": "babel src --out-dir=dist/ && node dist/index.js"
  },
  "author": "Horacio Bertorello",
  "license": "ISC",
  "dependencies": {
    "graphql": "^15.5.1"
  },
  "devDependencies": {
    "babel-cli": "^6.26.0",
    "babel-plugin-transform-object-rest-spread": "^6.26.0",
    "babel-preset-env": "^1.7.0"
  }
}
